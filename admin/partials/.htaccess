# Partials de layout, solo se incluyen desde otros PHP del panel, nunca se sirven directo.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
