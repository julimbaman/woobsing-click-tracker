# Bloquea el acceso web directo a archivos que nunca deben servirse.
<FilesMatch "^(config\.php|clicks\.log|\.git.*)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
