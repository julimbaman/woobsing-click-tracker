# Estos archivos son clases PHP de uso interno (require/include), nunca deben
# ejecutarse ni servirse directamente por HTTP.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
